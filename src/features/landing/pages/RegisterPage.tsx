import { Box } from '@mantine/core';
import { RegistrationForm } from '../forms';

const RegisterPage = () => {
  return (
    <Box>
      <RegistrationForm />
    </Box>
  );
};

export default RegisterPage;
